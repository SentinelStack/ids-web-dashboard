export const environment = {
  production: true,
  apiBaseUrl: 'https://qa-api.puk3p.online/api',
  // Google OAuth 2.0 Web client id (…apps.googleusercontent.com). Paste the one
  // from Google Cloud Console here to light up the "Sign in with Google" button.
  googleClientId: '',
};
